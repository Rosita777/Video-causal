# Main figures

Planned stable filenames:

- fig_task_teaser.pdf: source, receiver, and lingering causal footprint.
- fig_method_overview.pdf: structured pair, source-slot randomization,
  counterfactual distillation, and preservation.
- fig_qualitative_main.pdf: frozen temporal examples covering an explicit
  causal case, an implicit causal case, and a specificity case. The
  metadata-only rule selects case types, not successful outcomes.

Every figure must record its generating script, source manifest, selection
rule, and artifact hash in an adjacent metadata file. Do not add a manually
selected replacement after inspecting method outputs.
