# Editable Figure 1 sources

- fig_task_method_approved.pptx is the user-approved reference-style input.
- fig_task_method_overview.pptx is the publication version. It retains the
  composition while shortening small labels, setting at least 24 pixel text,
  and embedding the original vector icon paths.
- LUCIDE_LICENSE.txt covers the ten Lucide icons. All remaining schematic
  scene geometry, prompt labels, and connectors are native slide objects.

The adjoining figure manifest records exact input and output hashes. It uses
repository-relative paths and carries no result-media selection or score data.
