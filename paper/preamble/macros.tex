% Stable paper terminology. Baseline names and metric symbols should be changed
% here rather than retyped inconsistently throughout the manuscript.
\newcommand{\methodname}{Source-slot Randomized Counterfactual Distillation}
\newcommand{\methodshort}{\textsc{SRCD}}
\newcommand{\ces}{\ensuremath{\mathrm{CES}}}
\newcommand{\su}{\ensuremath{\mathrm{SU}}}
\newcommand{\matchedcontrol}{Matched Control}
\newcommand{\negativeprompt}{Negative Prompt}
\newcommand{\videoeraser}{VideoEraser (official CogVideoX)}
\newcommand{\tvtwounlearning}{T2VUnlearning-adapted (ours)}
\newcommand{\safree}{SAFREE-CogVideoX}

% Main-only builds do not load appendix labels. Clean combined previews may
% also omit architecture-only appendices, so fall back gracefully until the
% referenced appendix has entered the build.
\newcommand{\appref}[1]{%
  \ifdefined\paperwithoutappendix
    the supplementary material%
  \else
    \ifcsname r@#1\endcsname
      Appendix~\ref{#1}%
    \else
      the supplementary material%
    \fi
  \fi}

% Architecture mode renders planning boxes. Clean main-only and anonymous
% submission wrappers define \paperprosemode before loading main.tex.
\newif\ifoutline
\ifdefined\paperprosemode
  \outlinefalse
\else
  \outlinetrue
\fi

\newcommand{\outlineblock}[2]{%
  \ifoutline
    \par\medskip
    \noindent\colorbox{blue!6}{%
      \parbox{\dimexpr\linewidth-2\fboxsep\relax}{%
        \textbf{#1.} #2}}
    \par\medskip
  \fi}

\newcommand{\claimboundary}[1]{%
  \outlineblock{Claim boundary}{#1}}
