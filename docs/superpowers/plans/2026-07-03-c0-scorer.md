# C0 Counterfactual Grid Scorer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a CLI scorer that converts C0 VLM predictions into variant-level and item-level validity tables.

**Architecture:** Add one standalone scoring script under `scripts/` and one focused pytest file. The script reads C0 prediction CSV rows, infers expected target/footprint states from the variant role, writes variant/item CSVs plus a JSON summary, and leaves generation/evaluation code unchanged.

**Tech Stack:** Python standard library, CSV/JSON, pytest, existing C0 fable prediction schema.

---

### Task 1: Scorer Behavior Tests

**Files:**
- Create: `tests/test_score_c0_counterfactual_grid.py`
- Create later: `scripts/score_c0_counterfactual_grid.py`

- [ ] Write tests for boolean normalization, expected state inference, variant pass/fail scoring, item aggregation, and CLI outputs.
- [ ] Run `PYTHONNOUSERSITE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /home/deepseek_VG/.conda/envs/vcecf/bin/python -m pytest tests/test_score_c0_counterfactual_grid.py -q` and confirm failure because the script does not exist.

### Task 2: Scorer Implementation

**Files:**
- Create: `scripts/score_c0_counterfactual_grid.py`

- [ ] Implement `normalize_bool`, `score_prediction_rows`, `aggregate_item_scores`, CSV writing, JSON summary writing, and CLI argument parsing.
- [ ] Run the focused tests and confirm they pass.

### Task 3: Pilot Scoring And Log

**Files:**
- Modify: `docs/experiment_log.md`

- [ ] Run the scorer on `experiments/evaluation/c0_counterfactual_grid_quality3_fable_20260703/fable_run/vlm_predictions.csv`.
- [ ] Write outputs under `experiments/evaluation/c0_counterfactual_grid_quality3_fable_20260703/c0_scores/`.
- [ ] Append the scorer result and next screening implication to `docs/experiment_log.md`.
- [ ] Run focused C0 regression tests.
